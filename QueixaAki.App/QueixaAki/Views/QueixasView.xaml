<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QueixaAki.Views.QueixasView"
             Title="Queixas"
             BackgroundColor="{StaticResource PrimaryColor}">

    <ListView ItemsSource="{Binding Queixas}" 
                  Margin="25"
                  IsPullToRefreshEnabled="True"
                  RefreshCommand="{Binding AtualizarCommand}"
                  IsRefreshing="{Binding Carregando}"
                  RefreshControlColor="{StaticResource SecondaryColor}"
                  SeparatorColor="{StaticResource SecondaryColor}"
                  HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical">
                        <Label Text="{Binding NomeArquivoCompleto}" 
                                       FontSize="20" 
                                       VerticalTextAlignment="Center"/>

                        <Label Text="{Binding Endereco.EnderecoCompleto}"
                               FontSize="14" 
                               FontAttributes="Bold"
                               TextColor="DarkGray"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>